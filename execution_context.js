// js code when it goes to browser, it is executed in the browser environment. The browser environment has 2 parts - engine and runtime. The engine has 2 parts - callstack and heap. The runtime has 3 parts - Web API, callback queue and event loop. The event loop prioritizes the microtask queue over the callback queue. If there is any task in the microtask queue, it will execute that first before executing the task in the callback queue.
// google enjine is v8, firefox engine is spider monkey, safari engine is JavaScriptCore, edge engine is Chakra. The engine is responsible for parsing the code and executing it. The runtime is responsible for providing the environment in which the code is executed. The runtime provides the Web API, callback queue and event loop. The Web API provides the setTimeout, setInterval, fetch, XMLHttpRequest, DOM manipulation and other APIs. The callback queue is a queue of functions that are waiting to be executed. The event loop is responsible for checking the callstack and the callback queue and executing the functions in the callback queue when the callstack is empty.
// interpretation vs compilation - interpretation is when the code is executed line by line, compilation is when the code is converted into machine code before execution. The engine can either interpret or compile the code. The engine can also use Just In Time (JIT) compilation which is a combination of interpretation and compilation. The engine will interpret the code and then compile it into machine code for faster execution. The engine can also use Ahead Of Time (AOT) compilation which is when the code is compiled into machine code before execution. The engine can also use a combination of AOT and JIT compilation. The engine can also use a combination of interpretation, JIT and AOT compilation.
// interpretation is slower than compilation because the code is executed line by line. Compilation is faster than interpretation because the code is converted into machine code before execution. JIT compilation is faster than interpretation because the code is compiled into machine code for faster execution. AOT compilation is faster than JIT compilation because the code is compiled into machine code before execution. The engine can also use a combination of interpretation, JIT and AOT compilation for optimal performance.
// comparison of interpretation and compilation - interpretation is slower than compilation because the code is executed line by line. Compilation is faster than interpretation because the code is converted into machine code before execution. JIT compilation is faster than interpretation because the code is compiled into machine code for faster execution. AOT compilation is faster than JIT compilation because the code is compiled into machine code before execution. The engine can also use a combination of interpretation, JIT and AOT compilation for optimal performance.
// jit (just in time) compilation is a combination of interpretation and compilation. The engine will interpret the code and then compile it into machine code for faster execution. AOT (ahead of time) compilation is when the code is compiled into machine code before execution. The engine can also use a combination of AOT and JIT compilation. The engine can also use a combination of interpretation, JIT and AOT compilation for optimal performance.
// execution context is the environment in which the code is executed. The execution context has 2 parts - global execution context and function execution context. The global execution context is created when the code is executed for the first time. The function execution context is created when a function is called. The execution context has 3 parts - variable object, scope chain and this. The variable object is an object that contains all the variables and functions defined in the execution context. The scope chain is a chain of all the variable objects in the execution context. The this is a reference to the object that is currently executing the code. The this can be different depending on how the function is called.
// in global execution context, the this refers to the global object which is window in browser and global in node.js. In function execution context, the this refers to the object that is currently executing the code. The this can be different depending on how the function is called. The this can be different in regular functions, arrow functions and methods. The this can also be different in strict mode and non-strict mode. The this can also be different in classes and objects. The this can also be different in event handlers and callbacks. The this can also be different in setTimeout and setInterval. The this can also be different in bind, call and apply. The this can also be different in constructors and prototypes. The this can also be different in modules and imports. The this can also be different in async functions and promises. The this can also be different in generators and iterators. The this can also be different in proxies and reflect. The this can also be different in symbols and iterables. The this can also be different in weakmaps and weaksets. The this can also be different in maps and sets. The this can also be different in arrays and objects. The this can also be different in strings and numbers. The this can also be different in booleans and nulls. The this can also be different in undefineds and NaNs.
// global/functional execution context both has 2 phase - loading/creation and execution phase. In loading/creation phase, the variable object is created and the variables and functions are hoisted. In execution phase, the code is executed line by line and the variables and functions are assigned values. The this is also assigned a value in the loading/creation phase. The this can be different depending on how the function is called. The this can be different in regular functions, arrow functions and methods. The this can also be different in strict mode and non-strict mode. The this can also be different in classes and objects. The this can also be different in event handlers and callbacks. The this can also be different in setTimeout and setInterval. The this can also be different in bind, call and apply. The this can also be different in constructors and prototypes. The this can also be different in modules and imports. The this can also be different in async functions and promises. The this can also be different in generators and iterators. The this can also be different in proxies and reflect. The this can also be different in symbols and iterables. The this can also be different in weakmaps and weaksets. The this can also be different in maps and sets. The this can also be different in arrays and objects. The this can also be different in strings and numbers. The this can also be different in booleans and nulls. The this can also be different in undefineds and NaNs.
// parent scope cannot access child scope but child scope can access parent scope. This is called lexical scoping. The scope chain is created in the loading/creation phase and it is used to resolve the variables and functions in the execution phase. The scope chain is a chain of all the variable objects in the execution context. The scope chain is created when the function is called and it is destroyed when the function returns. The scope chain is used to resolve the variables and functions in the execution phase. The scope chain is a chain of all the variable objects in the execution context. The scope chain is created when the function is called and it is destroyed when the function returns. The scope chain is used to resolve the variables and functions in the execution phase. The scope chain is a chain of all the variable objects in the execution context. The scope chain is created when the function is called and it is destroyed when the function returns. The scope chain is used to resolve the variables and functions in the execution phase. The scope chain is a chain of all the variable objects in the execution context. The scope chain is created when the function is called and it is destroyed when the function returns. The scope chain is used to resolve the variables and functions in the execution phase.